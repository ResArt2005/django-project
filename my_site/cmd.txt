cd /mnt/c/users/resar/Documents/GitHub/django-project/my_site
docker-compose up -d
docker compose down
docker restart django_app
docker exec -it django_app bash
py -3.13 _
docker exec -it django_db psql -U postgres -d django_db
docker compose exec web python manage.py createsuperuser
http://localhost:8001/admin/
http://localhost:8001


# Остановим и удалим тома
docker compose down -v

# Пересоберем
docker compose build --no-cache

# Запустим
docker compose up -d

# Соберем статические файлы
docker compose exec web python manage.py collectstatic --noinput

# Проверим
curl http://localhost:8001/admin/

# Деактивируйте текущий venv (если активен)
deactivate

# Удалите старый venv
rm -rf env

# Создайте новый
python -m venv env

# Активируйте
source env/bin/activate

# Установите зависимости
pip install -r requirements.txt